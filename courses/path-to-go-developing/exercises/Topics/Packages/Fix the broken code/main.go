pkg main

import "format"

func init() {
    fmt.Println(emoji())
}

// DO NOT change the code of the emoji() function! look out for other errors!
func emoji() string {
	// return an emoji
	return "👨‍💻" + " 🏆 Well done! 🏆 " + "👩‍💻"
}